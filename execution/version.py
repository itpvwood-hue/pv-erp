"""
PVWood ERP — single source of truth for application version.

Versioning scheme: MAJOR.MINOR.PATCH
  MAJOR — incompatible schema or API breaks (bump rarely)
  MINOR — new features, backwards-compatible (e.g. new module)
  PATCH — bug fixes / small UX tweaks (bump for every shipped change)

When shipping any change, bump PATCH (or MINOR/MAJOR if warranted), update
BUILD_DATE to today, and prepend a 1-line entry to CHANGELOG below.
"""
from datetime import date

VERSION    = "2.22.4"
BUILD_DATE = "2026-06-29"

# Newest first. Format: ("X.Y.Z", "YYYY-MM-DD", "one-line description")
CHANGELOG = [
    ("2.22.4", "2026-06-29", "FC Re-grade modal: multi-line + searchable. The modal was a single source→target→qty form with plain dropdowns (555 veneers, hard to find). It's now a row-based form where each row has its OWN search box on BOTH the source and target selectors (filters by code/name/species/grade/cut), plus a qty. 'Add another re-grade' adds rows and each row has a remove button; the summary lists every pending move and Confirm posts them all (each via /api/fc/regrade, sequentially, reporting how many succeeded/failed). Sources are veneers physically at FC (fc_stock>0); targets are any veneer. Card 'Regrade' buttons still pre-select that veneer as the first row's source. Costing is unchanged (FC basis blends; warehouse price untouched until veneers leave FC). Frontend-only. Verified in a preview on a copy of the live DB: source list = FC veneers, target list = all 555 veneers, target search narrowed 555→23, add-line gives 2 rows, and a 2-row submit applied both regrades (sources −30/−20, targets +30/+20) with both targets' unit_cost unchanged and the modal closing; no console errors."),
    ("2.22.3", "2026-06-29", "FC veneer cost model: regrades no longer move the warehouse price; the cost is realised only when veneers LEAVE FC. New materials.fc_unit_cost column holds a per-veneer FC cost basis (weighted average) kept separate from the global unit_cost. (1) Regrade (create_veneer_regrade) now blends the TARGET's fc_unit_cost from the source's FC basis and leaves unit_cost untouched on both sides — so repeated FC regrading never disturbs the warehouse/BOM price. (2) Veneers entering FC (WH→FC transfer fulfil) seed/blend fc_unit_cost from the WH unit_cost. (3) Veneers leaving FC realise the FC basis into the warehouse unit_cost via weighted average against WH stock: on FC→WH return (fulfil outbound) qty joins WH and unit_cost re-blends; on FC→production (save_veneer_alloc_and_confirm deduct) unit_cost re-blends the same way while WH qty stays (pieces consumed). All scoped to veneer_sheet; boards/resize unchanged. fc_unit_cost defaults to unit_cost when unset (self-healing, no backfill). The FC daily report values FC stock at fc_unit_cost. (4) Report: the FC daily report no longer lists internal re-grades — only material entering/leaving FC and the final grade on hand. Verified on a copy of the live DB with exact math: regrade 40→80→85 FC basis with WH cost held at 40; FC→WH return blends unit_cost to 76; production consume to 78.5714 (WH qty unchanged); WH→FC entry seeds FC basis to 100; via the UI a regrade left the target's unit_cost unchanged while setting fc_unit_cost; report omits regrades; no console errors."),
    ("2.22.2", "2026-06-29", "BOM edit is now in-place. Clicking the pencil on a FG BOM card used to load the recipe into the BOM Builder panel at the TOP of the page, forcing a scroll up. editBomCard now relocates the (singleton) BOM Builder panel directly beneath the edited card and expands it there, so the user edits without leaving the row. The panel's home position (just above the list) is captured once and restored before every list re-render — so re-rendering bom-list's innerHTML never destroys the relocated panel — and the 'New / Edit BOM' button still opens it at the top. On a successful save the list re-renders (panel restored home) and the editor collapses, so the in-place edit closes cleanly. Frontend-only. Verified in a preview on a copy of the live DB: edit relocates the populated builder under the card, switching cards moves it, search re-render keeps it alive, the New button opens at top, and a save round-trips (no error, editor collapses, card persists) with no console errors."),
    ("2.22.1", "2026-06-29", "Four FC/BOM fixes. (1) Veneer Re-grade button did nothing — fcOpenRegradeModal set .value on rgr-from-loc/rgr-to-loc, hidden inputs that no longer exist in the modal, throwing 'Cannot set properties of null' before the modal opened. Removed those dead lines (regrade is always fc_station→fc_station, hard-set in create_veneer_regrade; the location fields were never sent). (2) Glue recipes couldn't be deleted — DELETE /api/glue-recipes/{id} ran 'UPDATE bom SET glue_recipe_id=NULL', but the legacy `bom` table was dropped in v2.21.79, so every delete 500'd with 'no such table: bom'. Removed that line. (3) BOM glue 'hard-coded 45 g/face' — save_bom_for_sku did float(usage or 45) and the BOM Builder auto-filled 45 on glue pick, so any BOM saved/uploaded without a glue value silently became 45 (uploads WITH a value already stored it correctly — the live DB has 357.5, 390, 200… across BOMs). Now the provided g/face is stored verbatim and a missing value stores NULL (shows '—', 0 glue cost) instead of a misleading 45; the builder leaves the glue qty blank on pick (boards/veneers still default to pallet qty). (4) M2 veneers now show just the standard length (e.g. '2500') in the Materials W×L column instead of 'var×2500' — width is known to vary for M2-counted veneers. Verified in a browser preview on a copy of the live DB: regrade modal opens + populates, a legacy 'Glue 9' recipe deletes (15→14), glue saves 200→200 / blank→NULL (never 45), M2 veneer shows '2500' while Pcs veneer shows '1265×2520', no console errors."),
    ("2.22.0", "2026-06-29", "FC Hub: two new features. (1) Board resizing — cut/resize FC board stock from one code into another. Mirrors the veneer regrade but with INDEPENDENT in/out quantities (yield): consume qty_in of the source board, produce qty_out of the target (one sheet can yield several smaller boards + offcut waste). Both stay in fc_stock; costing is value-conserving (incoming value = qty_in × source unit_cost, weighted-average-blended into the target's unit_cost; source per-unit cost unchanged). New board_resize_log table (idempotent CREATE in init_db), create_board_resize/get_board_resize_log, POST /api/fc/resize + GET /api/fc/resize-log, a 'Resize Board' button + modal on the FC Inventory tab and a per-card 'Resize' button on board cards, a Recent Resizes log, and RESIZE added to the unified FC movement log/filter. Guards: source must have enough FC stock, both materials must be core_board, distinct codes. (2) Daily printable report — a 'Daily Report' button on the FC Inventory tab opens a print/PDF view (reusing the shared SLH report styling) with current FC department stock (veneers + boards: code, species/grade or dims, qty, unit cost, value, totals) plus today's FC activity (transfers in, regrades, resizes, releases to laminating). Also added thickness_mm/width_mm/length_mm to the /api/fc/stock query so board dims show in the resize picker + report. Verified on a copy of the live DB (resize math, guards, movements) and in a browser preview (modal populates 9 sources/142 targets, submit moved −2/+5 and reblended cost 285→283.04, logged, in movements; report builds all 3 sections, no console errors)."),
    ("2.21.88", "2026-06-29", "Materials > Veneers: show the standard length for M2-counted veneers (was a bare '—'). Veneers sold/counted by area (unit=M2) have a varying width per piece but a standard length, so width_mm is left blank — and the W×L column rendered '—' even though length_mm is set (38 such veneers in the live DB). veneerRow in portal_warehouse.js now falls back to `var×{length_mm}` (e.g. 'var×2500') when width is absent but length is present, so the standard length is visible while still signalling the width varies; Pcs veneers and any veneer with both dims are unchanged (full 'W×L'). Frontend-only display tweak (boardRow left as-is — a board missing width is a data gap, not a variable-width item). Verified in a preview on a copy of the live DB: CEDAR XCEPZA01 + CHERRY XCHPZ201/XCHPZB01 now show 'var×2500', Pcs veneer XALPM201 still shows '1265×2520', no console errors."),
    ("2.21.87", "2026-06-29", "Fix: bulk BOM upload overwrote glue usage with the 45 g/face default. The Download Template names the glue column `face_glue_qty`/`back_glue_qty`, but the Export (and my v2.21.85 upload rewrite) used `face_glue_usage_g` — so a template-based CSV's glue value (e.g. 330 g/face) wasn't read and save_bom_for_sku fell back to 45. upload_bom now accepts EITHER column name (face_glue_qty OR face_glue_usage_g; same for back), and the Export header is aligned to the template's `face_glue_qty`/`back_glue_qty` so template + export + upload all round-trip on the same column. Verified: a CSV with face_glue_qty=330.3363 now stores 330.3363 g/face (was 45)."),
    ("2.21.86", "2026-06-26", "Fix: FG BOM showed ฿0/unit for boards + veneers when their cost was entered in `unit_cost` (the standard Materials cost field). get_structured_bom was the only BOM-cost path reading bare `m.price`, while the rest of the app (and an explicit code note 'price is a legacy fallback, use unit_cost first') uses COALESCE(unit_cost, price, 0). So materials with cost in unit_cost but price=0 displayed as ฿0 on the FG BOM card (glue was unaffected — it computes from the recipe via unit_cost). Both material-price reads in get_structured_bom (the core board/veneer lines + the packing lines) now use COALESCE(unit_cost, price, 0). Verified on a copy: a board with price=0, unit_cost=107 now shows ฿107/unit (was ฿0). NOTE: the reported SKU '4ROM52A21' does not exist in the project erp.db (which has 126 SKUs), so the running local server appears to point at a different database — but this code fix applies wherever it runs."),
    ("2.21.85", "2026-06-26", "Fix: restore the bulk BOM CSV upload (broken by the products/bom retirement, which had 410'd it). Rewrote /api/upload/bom to parse the one-row-per-FG-SKU format (same columns as Export CSV / Download Template) and write through the BOM Builder (skus + bom_lines) via save_bom_for_sku, instead of the retired products/bom tables. Per row it upserts the SKU + replaces its BOM lines (base board, face/back veneer, face/back glue, packing); component codes that don't resolve to a material/glue are reported as warnings (the line is skipped) rather than failing the whole import; the BOM list refreshes after upload. 'add' and 'replace' both upsert per SKU (neither deletes SKUs absent from the file). Verified e2e on a copy: a 2-row CSV created TEST-BULK-01 with correct prices (board 107, veneer 68.7, glue 26.86) and warned on a bad code. NOTE on the other reported symptom (BOM showing ฿0 for boards/veneers): not a bug — get_structured_bom/the card return + render the prices correctly against current data (verified 107/68.7); that screenshot was stale (its veneer qty differed), a page refresh shows the prices."),
    ("2.21.84", "2026-06-26", "FC (Feed Center) reclassified as a non-production node (structural cleanup, option 1). FC is a material-prep/QC/staging node, not a per-line production stage — so it's removed from the production boards + line flows: the Kanban columns (DEPTS) and Line-Board sections (LINE_FLOW) in index.html, the finance Kanban (KAN_DEPTS), and the catalog `line_flow` for the main lines (_DEFAULT_FLOW: P01/P02/P37 now start at laminating; the FC line keeps its own ['fc'] flow). FC stays a valid `departments` row + keeps its FC Hub: a batch still STARTS at the FC release gate (current_department='fc' is hard-set on batch creation, not from line_flow) and is released to laminating via /api/fc/laminating-material-request — that path is unchanged. Verified on a copy of the live DB: P01 flow + Kanban start at laminating with no FC column, the 5 batches at the FC gate still show in the FC Hub, fc remains a valid dept, no console errors. The label work (FC->'Feed Center') shipped in 2.21.83. Full model + remaining notes: directives/fc_station_department_model.md."),
    ("2.21.83", "2026-06-26", "FC relabelled 'Feed Center' everywhere (was inconsistently 'FC / Cutting' and, in core.js, 'FC / Glue Mixing'). FC is a material-prep/QC/staging node between the warehouse and the production lines (screens + grades/regrades incoming material, keeps components like VCMX, feeds approved material to the lines) — NOT a production stage. Label-only, no behavior/schema change: updated the catalog seed (departments.label + manufacturing_line.line_name, which init_db refreshes on existing DBs too) and the display labels/dropdowns (core.js + finance.html DEPT_LABEL, SLH_DEPT_LABEL, the role-scope options in admin.html/auth.js/portal_admin.js, and the dept/line <option>s in index.html). 'FC' is kept as the short code in compact spots (SLH_DEPT_SHORT). Verified: catalog + all label maps read 'Feed Center', no 'FC / Cutting'/'Glue Mixing' left in the DOM, no console errors. The deeper structural reclassification (remove fc from departments + per-line line_flow, settle the 'at-FC' gate state) is mapped in directives/fc_station_department_model.md and NOT done here."),
    ("2.21.82", "2026-06-26", "WS3b follow-ups (post products->skus migration). (1) FC material requirements: get_fc_material_requirements had an interim repoint that read bom_lines with COALESCE(qty_override, usage_g_per_face) as one 'quantity_per_unit' — flattening boards (sheets/pallet) and glue (g/face) into the same figure and (wrongly) including glue, which FC doesn't manage. Dropped that flawed primary path so the function uses only the canonical per-pallet computation (required = order_pallets x pcs_per_pallet x (1+waste); glue_formula skipped). Verified: a 1-pallet order of a 240-pcs/pallet SKU now needs 240 core boards + 252 veneer (5% waste), no glue rows. (2) Production Logs: the create flow still sent product_id while the reads key on sku_id (so new logs showed a blank product). The product dropdown already lists skus, so saveLog now sends sku_id, create_production_log stores sku_id, and ProductionLogIn takes sku_id. (editPoLine's product_id arg was found to be unused/harmless — left as-is.)"),
    ("2.21.81", "2026-06-26", "Fix: the VCMX pages were unreachable from the nav. The whole VCMX feature (VCMX Orders page + 'New VCMX Production Order', VCMX BOM Builder, VCMX-Lam station) is built and wired, but 'vcmx' and 'vcmx-lam' were never added to ROLE_PAGES for ANY role — so the nav stripped both links for everyone (incl. Managerial + Production Planning). Granted 'vcmx' (VCMX Orders, under Products & BOM) + 'vcmx-lam' (VCMX Laminating, under Production Module) to MANAGERIAL and PRODUCTION_PLANNING, and 'vcmx-lam' to DEPARTMENT_LEADER (they work the VCMX-Lam station). Verified: both nav links now show, the VCMX Orders page loads with its 'New VCMX Production Order' button, no console errors. Frontend-only (ROLE_PAGES); takes effect on browser refresh."),
    ("2.21.80", "2026-06-26", "Fix: material creation was silently broken + finish VCMX on the skus model. (1) DANGLING-FK HEAL: streamlining dropped several legacy tables (suppliers, production_table, prod_batch, mfg_order, products, bom) but other tables still declared a FOREIGN KEY at them — notably materials.supplier_id -> suppliers — so with FK enforcement on, INSERT INTO materials failed outright ('no such table: suppliers'), breaking material creation AND VCMX BOM creation. init_db now rebuilds any table whose FK points at a missing table, stripping just the dead FK clauses (inline + trailing incl. ON DELETE/UPDATE actions); idempotent, self-healing, indexes preserved. Verified: materials 905 rows intact, unique-code index intact, inserts work. (2) VCMX: _vcmx_ensure_paired_records now also UPDATEs the paired skus row's name/dims on rename (not just insert), and a duplicate guard line was removed. End-to-end verified on a copy: create VCMX BOM -> skus row created; create VCMX order -> shows name+SKU (no longer blank); regular create_material works again."),
    ("2.21.79", "2026-06-26", "Streamlining WS3b phase 3 (contract): RETIRE the legacy `products` + per-product `bom` tables. FG identity now lives entirely in `skus` (reached via sku_id); BOM lives in the BOM Builder (bom_lines). init_db rebuilds every table that still had a foreign key to products (po_lines, production_orders, orders, production_logs) to strip that FK + relax product_id NOT NULL (so rows key off sku_id; product_id kept nullable/vestigial), then drops products + bom (FK off + legacy_alter_table on; self-healing + idempotent; runs after the sku_id backfill). VCMX finished-goods now go into `skus` (unified catalog) — _vcmx_ensure_paired_records creates a skus row + VCMX orders set sku_id. Create flows (create_po_line, create_production_order) take sku_id directly. Removed: products + per-product bom CRUD functions + /api/products* + /api/bom* endpoints + ProductIn/BomIn/BomUpdate; the products auto-sync in save_bom_for_sku; the legacy BOM CSV upload (now 410). Repointed: the production-order material-requirements reader onto bom_lines (by sku_id), get_full_bom_context + dashboard count + PDF-PO-import validation onto skus. Frontend: preload loads skus; the PO-line picker + PDF-import + prod-order-from-PO-line create flows send sku_id. Verified on a copy of the live DB: app boots (304 routes), products/bom dropped, foreign_key_check clean, integrity ok, production orders/batches/po-lines read product_name+sku, and creating a PO-line / production order with sku_id alone works. KNOWN FOLLOW-UPS: editPoLine + the legacy production_logs capacity-log create still pass product_id; the bom_lines material-requirements quantity semantics (qty_override vs usage_g_per_face) want a real-data sanity check."),
    ("2.21.78", "2026-06-26", "Streamlining WS3b phase 2 (migrate reads + creates onto skus). Re-pointed ~48 production/accounting/report/daily-review queries from the legacy `products` table onto `skus` via the new sku_id (LEFT JOIN skus p ON p.id = X.sku_id — LEFT so VCMX orders, sku_id NULL, aren't dropped from lists), collapsing 10 double-joins (products + a second `LEFT JOIN skus sk ON sk.code=p.sku`) into one; p.sku->p.code (kept the output column name `sku` via AS), sk.pallet_qty/dimensions->p.*. The 5 reporting views now read skus directly off sku_id too. Create flows (create_po_line, create_production_order) now populate sku_id (transitional _sku_id_for_product bridge via products.sku==skus.code; accepts an explicit sku_id too). Verified on a copy of the live DB: production orders/batches/po-lines/accounting all return product_name+sku off sku_id, and newly-created lines/orders get sku_id (readback correct). KNOWN GAPS for phase 3: VCMX orders + legacy production_logs rows have no skus match (show blank — VCMX is disposable/recreatable per owner); the `products` table + legacy `bom` table/endpoints + the products auto-sync still exist (dropped in phase 3). The legacy `bom` reads were left on products (reverted) since that table has no sku_id and is being removed wholesale."),
    ("2.21.77", "2026-06-26", "Streamlining WS3b phase 1 (expand, additive — no behavior change): groundwork to move the FG identity off the legacy `products` table onto `skus`. Added a nullable sku_id column to production_orders, po_lines, orders and production_logs, and backfilled it in init_db from the legacy bridge (products.sku == skus.code, case-insensitive). Idempotent: only fills rows whose sku_id is still NULL and that have a matching sku. Coverage on the live data: po_lines 18/18, production_orders 18/20 (the 2 VCMX orders carry vcmx_bom_id and stay NULL), orders 0/0 (empty), production_logs 0/35 (legacy rows point at deleted products). Nothing reads sku_id yet — phase 2 re-points the ~60 production/accounting/report joins + the PO-line/prod-order create flows + preload onto skus, phase 3 drops products + the legacy bom table/endpoints."),
    ("2.21.76", "2026-06-26", "Streamlining WS3a (part 2): RETIRE the dead 'System A' tables prod_batch + mfg_order. The live Station Hub keys all production off batches/production_orders (System B); prod_batch held 2 stale rows, mfg_order was empty, and nothing live created or read them (the New-batch modal + prod_batch detail panel were orphaned, /api/prod-batches & /api/mfg-orders had no live callers). init_db now, with FK off, rebuilds every *_log table that still declared REFERENCES prod_batch(batch_id) — all 8 station logs incl. packing_log — to strip that dead FK (data preserved), then drops prod_batch + mfg_order (legacy_alter_table=ON so the rename skips view re-validation). Self-healing + idempotent: runs whenever prod_batch exists OR any table still carries a dangling prod_batch FK. Also FIXES the packing_bom view, which WS1's suppliers drop had left broken ('no such table: suppliers' on every Packing-BOM query) — recreated without the suppliers join. Removed the orphaned endpoints (/api/prod-batches*, /api/mfg-orders, laminating|repair/advance), their DB functions + pydantic models, the no-op 'UPDATE prod_batch SET status' lines in the log functions, the orphaned frontend (createNewBatch/openNewBatchModal, slOpenDetail/_renderProdBatchDetail/bdcSlSave, newBatchModal markup), and the prod_batch undo-revert. Verified on a copy of the live DB: station logging (cold-press + packing) writes succeed, reports work, foreign_key_check clean of prod_batch, integrity ok."),
    ("2.21.75", "2026-06-26", "Streamlining WS3a (part 1): FIX the Production Reports + Daily Review line-scoping. The Station Leader Hub logs production into the *_log tables keyed by batches.batch_number (the live System B), but the 5 reporting views (v_lam_efficacy, v_sanding_defect_rate, v_daily_production, v_ncg_by_reason, v_ncg_backtrack) + get_station_day_jobs still JOINed the legacy prod_batch table (System A) on batch_id — which the live logs no longer match — so Reports + NCG backtracking returned almost nothing and Daily Review couldn't attribute a job to its line. Re-pointed all of them onto batches JOIN production_orders (+ products for sku_code): line_id<-production_orders.production_line, sku_code<-products.sku, production_date<-DATE(log timestamp), shift blank (no System-B equivalent). Verified on a copy of the live DB: lam-efficacy returns real rows, the grading->batches->order->product chain populates daily/NCG views, and a P01 laminating job now correctly scopes to P01. View definitions are now DROP+CREATE so existing DBs pick them up on restart. No data migration; prod_batch/mfg_order retirement is the next step (WS3a part 2)."),
    ("2.21.74", "2026-06-26", "Streamlining workstream 2: the Kanban and Line Board are now read-only PLANNING viewers — production stage moves & splits happen only in the Station Leader Hub (logged via /api/production/*). Removed drag-and-drop and the on-card Move/Split buttons from both boards; cards now click through to a read-only batch detail. Planners can still tweak PRIORITY inline on every card (PATCH /api/batches/{id}/priority). The shared batch-detail offcanvas gained a read-only mode (deptBatchDetail(id,dept,true)) that hides Move/Split and shows a 'moves are done in the Station Leader Hub' hint; the dept pages, Station Hub and packing views keep their existing actions. Removed the now-dead kanbanDrop/lbDrop handlers."),
    ("2.21.73", "2026-06-25", "Streamlining workstream 1 (safe cleanup, no behavior change): removed the unused GM_COMPONENTS_NEW duplicate constant; dropped two dead tables (suppliers, production_table — never read/written) from the schema + a DROP migration for existing DBs; consolidated the two identical Station-Hub short-label maps into one SLH_DEPT_SHORT (derived from SLH_DEPT_LABEL). Note: legacy Products/BOM removal was DEFERRED — the sales PO-line flow still binds FG->products.id, so it needs a migration to skus, not a plain delete."),
    ("2.21.72", "2026-06-25", "Login: 'Keep me signed in on this computer' checkbox. When ticked the session lasts 30 days (vs the normal ~8.5h), so re-opening the app on the same computer skips the login. The token already persists in localStorage; this just extends the server session. LoginIn gained remember; login() passes hours=720 when set. The checkbox choice is remembered per browser. Default off."),
    ("2.21.71", "2026-06-25", "Fix: Glue BOM edits/deletes reverted on restart. _seed_real_glue_recipes ran on every startup and UPDATE-ed the 14 canonical recipes (wiping user edits + forcing is_active=1) or re-INSERTed missing ones (resurrecting deleted recipes), so a redeploy reset the Glue BOMs. It now seeds the defaults ONLY when the recipe catalog is empty (fresh DB); after that the user owns their Glue BOMs and edits/deactivations/deletions persist. Legacy GLU-% placeholder cleanup still runs."),
    ("2.21.70", "2026-06-25", "Material delete now covers ALL references (was only checking lots/glue/BOM, so other FKs hit the generic 'still referenced elsewhere'). delete_material dynamically scans every FK pointing at materials(id) via PRAGMA foreign_key_list: a normal delete names exactly what blocks it (stock, movements, requests, lots, documents, NCG/regrade/cost history, …); Managerial Force delete clears every operational reference + glue links (foreign_keys OFF during the sweep) then removes the material. Product-recipe references (bom_lines/bom/vcmx_boms/packing_lines) still block even on force, naming the recipe/SKU."),
    ("2.21.69", "2026-06-19", "Materials: Managerial 'Force delete' for materials stuck on stale references. A normal delete still refuses when a material is used (now names which: received lots, glue recipes, or BOM SKUs). When the only blockers are received-lot records and/or glue-recipe links, a Managerial user is offered Force delete, which clears those then removes the material (DELETE /api/materials/{id}?force=true, Managerial-only). BOM usage is never force-cleared — it names the SKUs so you remove it from the product BOM first. The materials delete endpoint now also requires auth."),
    ("2.21.68", "2026-06-19", "Enforce unique material codes (SKU codes already UNIQUE). (a) _mat_by_code now matches case-insensitively so an upload of 'abc' updates the existing 'ABC' instead of making a near-duplicate; (b) create_material rejects a code that already exists; (c) the inventory upload rejects rows whose code duplicates another row in the same file (case-insensitive); (d) a best-effort case-insensitive UNIQUE index on materials(code) is created at startup (skipped if pre-existing duplicates block it, then app-layer still prevents new ones). To find existing dupes: SELECT code,COUNT(*) FROM materials WHERE COALESCE(code,'')!='' GROUP BY code COLLATE NOCASE HAVING COUNT(*)>1."),
    ("2.21.67", "2026-06-19", "Glue recipe editor: relabel hardcoded additive fields — 'E0 Glue (Urea Resin) kg' -> 'Glue kg', 'Latex G312 kg' -> 'Latex kg'. Label-only (the underlying field ids/keys e0_glue/latex_g312 are unchanged, so recipes + material links are unaffected)."),
    ("2.21.66", "2026-06-19", "One-time glue-mix stock cleanup. New managerial action (User Management → 'Run cleanup', POST /api/maintenance/fold-glue-stock) folds the legacy blank-line glue_mix station-stock rows from the pre-2.21.64 deduction bug into the P01 row (summing, e.g. -157.79 blank + 1000 P01 = 842.21) and re-lines their movements to P01, then deletes the blank rows. Idempotent. Fixes the negative 'On Hand' in the Glue Mix shortfall without re-counting."),
    ("2.21.65", "2026-06-19", "Fix: Daily Review didn't refresh on date change. slhRefresh() handled every Station Leader Hub tab except 'review', so changing the header date + clicking Refresh left the Daily Review pane stale. Added the 'review' branch; also wired the date picker's onchange to reload the review when that tab is active."),
    ("2.21.64", "2026-06-19", "Fix 3 Glue Mixing bugs. (1) Linked materials weren't recognised: the station resolver looked up material_links by the short key (e0/latex/yellow) but the Glue BOM Builder + backend store them field-keyed (e0_glue/latex_g312/yellow_pigment) — now resolves by the field key (legacy short key kept as fallback). (2) Confirm-mix didn't deduct station stock: the deduction hardcoded line_id='' while glue-mix stock is per-line (P01), so it hit a phantom row. gmSubmitMix now sends the station line, the backend deducts on it, and gmRefreshStation reads it — deposit/read/deduct all line-consistent. (3) Batch chip showed 'N pcs' for what is the pallet count — relabelled 'N pallets'."),
    ("2.21.63", "2026-06-19", "FC / Cutting is now its own independent line in the Station Leader Hub, not P01. Selecting FC scopes the station to the 'FC' line (matches the FC station's keying) and locks the line selector; header/chip read 'FC LINE · Cutting' instead of 'P01 · FC'. The FC batch list is centralised (shows batches from every production line — all P01/P02/P37 batches pass through FC for cutting, then feed back to their line), like Packing. FC added as a selectable line; station stock/movements/requests key to 'FC'. Frontend-only."),
    ("2.21.62", "2026-06-19", "Fix: Glue Mixing shortfall WH requests had no parent line. slgmRequestFromWH and slgmBulkRequestWH ('Request All Short + Low from WH') posted consumable requests with line_id='' — so they never appeared in the station's line-scoped 'My Open Requests', and showed as '(no line)' in the warehouse supply queue. Both now carry the station's current line (new _slgmLine() helper, mirrors stOpenWHRequest). Glue mix is per-line, so requests now correctly attach to e.g. P01."),
    ("2.21.61", "2026-06-19", "Daily Review: deeper downstream reconciliation (adjacent-sync + flag). Correcting a station's output quantity now (a) sets the immediately-next station's pcs_in to match for the same batch (only where that station has a clean pcs_in gate: laminating->cold_press, sanding->hot_press, etc.), and (b) flags every remaining downstream job log of that batch as 'review needed' (new station_job_flags table) so the leader confirms each — safe across the non-pass-through Repair (rework) and Grading (A/B/NCG/Reject) stations. Flagged rows show a 'review' badge in the Daily Review and highlight; saving a job clears its own flag. Batch reassignment flags the new (and old) batch's downstream chain. All audited via action_log."),
    ("2.21.60", "2026-06-19", "Daily report overhaul + Phase 2 corrections. REPORT: the two reports merged into one combined daily sheet, now Thai+Chinese (no English), with a company header (PVWood logo + name; address editable in _RPT_COMPANY), Production jobs (with NCG/defect column+total for grading/sanding), station stock Opening/Change/Closing balances, stock movements, consumable requests, and a bilingual signature block. Single 'Daily Report' button; filename {date}_{LINE}_{DEPT}_daily. CORRECTIONS: new 'Daily Review' tab in the Station Leader Hub lists the day's completed jobs + stock movements with edit-in-place fields — fix a job's quantity, reassign its batch, fix a movement's quantity (station stock auto-reconciles by the delta), or fix a movement's batch ref. Every change is written to the audit log (action_log). Backend: get_station_day_balances(), defect col in get_station_day_jobs(), correct_station_job()/correct_stock_movement(); endpoints PATCH /api/station/job/{dept}/{log_id} and /api/station/movement/{id}; /api/station/daily-report now returns balances."),
    ("2.21.59", "2026-06-19", "Daily station reports (print -> Save as PDF, physically signed). New 'Daily Report' dropdown + date picker in the Station Leader Hub header generates two print-optimised reports for the selected station/line/date: (1) Production Report — completed-job logs (batch, qty, operator, notes, total); (2) Stock & Utilization Report — station stock movements + consumable requests raised. Each opens a clean A4 print window with a signature block; the window title is set to a systematic filename (YYYY-MM-DD_LINE_DEPT_production|stock) so Save-as-PDF pre-fills the archive name. No digital sign-off and no new server dependency. Backend: get_station_day_jobs() over the per-department log tables + GET /api/station/daily-report. (Researched: Thai Revenue Code s.87 requires a goods/raw-materials report within 3 working days — these reports support that; confirm exact form with your auditor.)"),
    ("2.21.58", "2026-06-19", "Station Leader Hub: removed the duplicate non-scoped 'Request Consumables' header button (added in 2.21.55). Consumable requests now go solely through the scope-locked 'Request from Warehouse' button on the Stock & Movements tab (stOpenWHRequest), which is tied to the selected station + line. Avoids the confusion of two request buttons where one wasn't locked to the chosen department/line."),
    ("2.21.57", "2026-06-19", "Reconcile config drift: commit the FINANCE role option to the repo. The User Management Role dropdown's 'Finance (Accounting + Purchasing portal)' option existed only as an uncommitted manual edit on the server (never in git, per git -S), so any deploy would have wiped it. Added it to index.html's um-role + the frontend ROLE / ROLE_LABEL maps. FINANCE was already a valid backend role (CHECK constraint + Role.FINANCE) with a dedicated Finance Portal (finance.html at /finance); Finance users sign in there, the main-app dropdown just lets an admin create the account. No backend change."),
    ("2.21.56", "2026-06-19", "Role dropdown: FC / Cutting, PUV, PVS, PSP are now selectable as scoped station-leader roles in User Management. Picking one creates a Department Leader auto-locked to that station/line (no manual department grid; an info note explains it), and round-trips on edit. Under the hood they save as DEPARTMENT_LEADER + a fixed assignment (FC->FC all-lines; PUV/PVS/PSP->a single generic 'Production' station on that line, pending their real flow). The Station Leader Hub renders the matching scope button and tolerates the generic 'production' station. No backend/role-constraint change. Verified e2e: creating each preset stores the right role+assignment; edit shows the preset back; the scoped user lands on their station in the Hub."),
    ("2.21.55", "2026-06-19", "Station Leader Hub access scoping. For Department Leaders the free-choice Station + Line dropdowns are now hidden and replaced by buttons for ONLY the (station, line) scopes they're assigned to (slhRenderScopeButtons; auto-selects the first). Managerial/Planning keep the dropdowns. Also removed the standalone 'Request Consumables' Supply nav item — consumable requests are now raised via a 'Request Consumables' button in the Station Leader Hub header (opens the same request modal). Frontend-only."),
    ("2.21.54", "2026-06-19", "Station-leader access tweaks. (1) Department Leaders no longer have FC Hub — removed from ROLE_PAGES + nav (nav-fc-hub). (2) FC / Cutting is now a station option in the Station Leader Hub department dropdown, so FC is reached there instead of the separate Hub page. (3) PUV/PVS/PSP are now treated as independent lines using the standard Hub UX (relabelled 'Independent lines'; the old request-only aux short-circuit in slhSetScope is removed). Their distinct production flow is deferred per request — for now they reuse the lamination Hub UX. No backend change."),
    ("2.21.53", "2026-06-19", "Login: a deactivated account now returns the clear 'Account is inactive' (403) instead of a misleading 'Invalid username or password' (401). get_user_by_username filtered active=1, so login could never reach its own inactive check — a disabled user looked exactly like a wrong password. Removed that filter (login is its only caller). Helps diagnose 'user can't log in' reports: wrong password = 401, disabled account = 403. Verified: active+correct 200, active+wrong-pw 401, deactivated 403, unknown user 401."),
    ("2.21.52", "2026-06-19", "Add Material modal now exposes veneer attributes. Picking type='Veneers' reveals a Veneer Attributes section (Species, Cut Type, Grade, Matching as dropdowns that suggest existing values but accept new ones, plus Face/Back and FSC selects) and the Dimensions section (thickness/width/length, previously board-only). These were columns the importer/export already used but the UI couldn't set — so UI-created veneers were missing them. Backend MaterialIn + create_material/update_material now persist species/cut_type/grade/matching/face_back/fsc. Verified e2e: adding a veneer via the form stores all attributes; dropdowns list existing species/grade/etc.; non-veneer types are unaffected."),
    ("2.21.51", "2026-06-19", "Consolidate duplicate PR-creation modals. The planning 'New PR (Stock Building)' modal (msfAdhocModal) was a second copy of the multi-material modal — it now reuses the shared #newPRModal (which is a superset: multi-material + split deliveries), pre-seeding a '[Stock-building, no PO]' note so Purchasing can still tell these apart. Removed the duplicate modal markup + ~190 lines of dead msfAdhoc* JS. Left the genuinely context-specific launchers untouched (they pre-fill a specific material and aren't duplicates): the Material-Shortfall row 'raise PR' (carries source_po_id + shortfall context) and the warehouse low-stock reorder + self-service single-item modals. Verified e2e: Stock Building button opens the shared modal with materials loaded and the note seeded; multi-line submit creates grouped PRs."),
    ("2.21.50", "2026-06-19", "Fix: New Purchase Request modal showed no material dropdown and no way to add lines. The modal's priming (load materials + render the first line) was registered inside a DOMContentLoaded handler, but the accounting portal script is fetched dynamically after login — DOMContentLoaded had already fired, so the show.bs.modal listener never attached and pr-lines-container stayed empty. Now wired immediately (idempotently) when the script loads. The modal already supported multi-material + split deliveries; both work again: the raw-material dropdown is populated from /api/materials and 'Add Material' adds more lines, each becoming its own PR under a shared group number. Verified e2e: dropdown lists materials, two lines submit as 2 PRs sharing one PRG group with type/uom auto-filled."),
    ("2.21.49", "2026-06-19", "Issuing a PO now captures the order price. The Approved->PO Issued button no longer silently flips status; it opens an Issue Purchase Order modal showing the material's current price in stock + on-hand qty, with a required 'Order unit price' field (prefilled with the in-stock price, or the previously-agreed price if re-issuing) and a live order total, plus the optional supplier PO reference. The agreed price is stored on the PR (purchase_requests.unit_cost) so received lots inherit it, and now shows as an '@ ฿x' badge on the PR row. Backend: list_purchase_requests surfaces stock_unit_cost + stock_on_hand; PRStatus accepts unit_cost and the status endpoint passes it through. Verified e2e: issuing requires a price, stores it, and it appears on the row."),
    ("2.21.48", "2026-06-19", "Material exports/imports now include the WLWH stock bucket (wlwh_stock column). All four material exports (veneers, boards, consumables, all-materials) emit current_stock, fc_stock AND wlwh_stock; the bulk importer writes wlwh_stock when present. This makes the export->set stock 0->import migration zero ALL three buckets (WH+FC+WLWH) instead of leaving WLWH stock behind — so a material parked in WLWH can now be fully zeroed and become purge-eligible in a Replace (mirror) upload. Blank cells still preserve existing values; a literal 0 zeroes the bucket. Note: purge still keeps any material that has a BOM line, received lots, or a glue link regardless of stock."),
    ("2.21.47", "2026-06-17", "Bulk material upload Replace = mirror. Replace mode now imports the file AND purges materials of the SAME category that aren't in the file — but only the truly unused ones (no BOM line, no stock in any bucket, no received lots, no glue link); anything still in use is kept and reported. Scope is the categories present in the upload, so a Veneers file never touches boards/consumables. Replaced the old unsafe delete-then-insert (which lost ids and FK-failed on BOM-referenced codes) with a plain upsert + purge_unused_materials(). The UI confirms before the destructive step and shows what was deleted vs kept-in-use. Verified e2e (function + HTTP): unused veneer not in file deleted; one with stock and one in a BOM kept; boards untouched; new rows added."),
    ("2.21.46", "2026-06-17", "WH<->WLWH stock movement is now request->fulfil. The move button no longer shifts stock immediately; it raises a PENDING request (new wh_move_requests table). A new 'Movement Fulfilment' page (Warehouse + Managerial) lists pending requests with each material's live available qty (flags shortfalls); a warehouse employee clicks Fulfil and ONLY THEN does stock move WH<->WLWH (validated against live stock at fulfil time — fails clearly if short), or Cancel to drop it with no move. A nav badge shows the pending count. Endpoints: POST/GET /api/warehouse/move-requests, POST .../{id}/fulfill|cancel; the fulfilment is undoable (reverses the stock move and returns the request to PENDING). Verified e2e: request leaves stock untouched (100/0), fulfil moves it (70/30 then 80/20), badge/queue update, double-fulfil + insufficient-stock guards, undo restores stock + request."),
    ("2.21.45", "2026-06-17", "Packing BOM: pick consumables from a dropdown. Adding a material to a packing spec used a free-text 'Material code' box, so users had to know/type codes. The packing-spec builder now loads packing-type materials (packing + consumable/other, packing first) and the line row's material field is a dropdown of '[CODE] Name (unit)'; selecting one auto-fills the unit. Empty-state prompts to add packing materials in Raw Materials. Verified e2e: dropdown lists the packing consumables, unit auto-fills, line adds correctly."),
    ("2.21.44", "2026-06-17", "Separate Bleaching formulas from Glue. Both compound types share the glue_recipes table, but there was no type tag and both the Glue and Bleaching tabs fetched the identical unfiltered list — so they showed the SAME rows and deleting a 'bleach' formula removed the glue one (it was one record in two places). Added a `kind` column ('glue'|'bleach'; existing rows backfill to glue). get_glue_recipes[_with_ingredients] + their endpoints take a kind filter; each tab now lists/creates/deletes only its own kind; new bleach formulas are tagged bleach, new glue stays glue, and the glue-mix station + glue editor fetch kind=glue so bleach never leaks into glue mixing. Verified e2e: glue tab and bleach tab are independent; deleting a bleach recipe leaves glue intact."),
    ("2.21.43", "2026-06-17", "Harden PAGE_LOADERS against stale-cache load-order skew. A browser running a cached/out-of-order nav.js could hit 'Uncaught ReferenceError: PAGE_LOADERS is not defined' at the inline Object.assign, because nav.js's `const PAGE_LOADERS` is a lexical global not visible as a window property. nav.js now mirrors it onto window (const PAGE_LOADERS = window.PAGE_LOADERS = window.PAGE_LOADERS || {}) and the inline registration self-initialises window.PAGE_LOADERS — so a bare PAGE_LOADERS reference always resolves and every script extends the SAME object. Verified: same object across nav + window, all 40 portal loaders register after login, no console errors. (The current code already loaded clean; this prevents the error from a half-cached deploy. Affected users should hard-refresh once.)"),
    ("2.21.42", "2026-06-17", "Factory Assistant chat history. Conversations were already persisted server-side (fa_conversations) but the UI threw the active session id away and only kept the chat in memory, so leaving/reloading the page lost the visible chat and there was no way back to past chats. Revamped: the active session id now persists in localStorage and its transcript is restored on load (survives reload + navigation); a 'Your Chats' sidebar lists every saved session (title from the first message, relative time, msg count) — click to reopen and continue, trash to delete; 'New chat' starts a fresh thread. New endpoints GET /api/factory-assistant/sessions, GET /history/{session_id}, DELETE /sessions/{session_id}, all scoped to the signed-in manager (a session id can't read another user's chat). Verified e2e: sidebar lists + opens sessions, full transcript restores after a hard reload, new/delete work, cross-user history blocked."),
    ("2.21.41", "2026-06-17", "Glue cost from raw-material unit_cost + Material/BOM delete. (1) Glue recipes showed cost-per-kg 0 when ingredients were linked to raw materials: _recipe_to_summary and _glue_recipe_cost_per_kg only read materials.price, but raw-material cost lives in unit_cost (set by receiving/edit/regrade). Both now use COALESCE(unit_cost, price, 0), so glue codes and the BOM costs that roll up glue now price correctly. (2) Materials and BOMs had no delete UI at all. Added a trash button to every Raw Materials row (delete_material now refuses with a clear reason when the material is used by a BOM line, received lot or glue recipe — instead of a raw FK 500) and a trash button to each BOM card on the Bill of Materials page (new DELETE /api/fg-bom/{sku} removes the SKU + its recipe, blocked if the SKU has sales/production orders). Verified e2e: glue cost 12.0 from unit_cost; unreferenced deletes succeed, referenced ones blocked with a message."),
    ("2.21.40", "2026-06-17", "Fix three fresh-DB 500s + Undo Phase 3 (NCG/scrap). FIXES: (1) Reports pages 500'd because v_daily_production/v_lam_efficacy/v_ncg_by_reason/v_sanding_defect_rate/v_ncg_backtrack were created on dev but never added to init_db — now created idempotently on startup. (2) Finance PO-lines + PO-line create/update 500'd because po_lines was missing packing_sku_id + pcs_per_pallet — added as migrations. (3) Deleting a glue recipe 500'd when a glue-only BOM line referenced it (the 'material_id OR glue_recipe_id' CHECK blocked nulling) — those lines are now deleted instead. (4) Saving a NEW material left the form stuck open: saveMaterial used Modal.getInstance (null when opened programmatically) -> getOrCreateInstance. (The 'BB is not defined' label was a mis-diagnosis; there is no BB in the code.) UNDO: the per-user Undo button now also reverses NCG flag (returns stock + deletes the item), NCG disposition (restores remaining/status, pulls qty back out of the target + restores its blended cost, deletes a resize-created SKU), and scrap entries. Verified e2e."),
    ("2.21.39", "2026-06-17", "Undo Phase 2: warehouse actions. The per-user Undo button now reverses six more action types, each returning a precise reversal plan: stock receive (deletes the lot, deducts the stock it added, restores the shipment + PR status; a quick-receive deletes the shipment it created), WH<->WLWH stock move (reverses the bucket shift + deletes the transfer log), FG<->WLWH move (restores each batch's fg_location + deletes the move log), supply-queue consumable fulfilment (returns WH stock, restores the request, deletes the cost-ledger row), and FC transfer fulfilment (reverses the WH<->FC shift + restores the request). Endpoints strip the internal undo plan from their responses; the undo executor still only touches whitelisted tables. Verified e2e (function + HTTP): every path reverses stock, lots, request/shipment/PR status and logs. Note: the 'Receive into WH/WLWH' selector for boards/veneers already shipped in 2.21.25. Next: NCG/scrap + other actions."),
    ("2.21.38", "2026-06-17", "Undo Phase 1: per-user 'Undo my last action' for station production logs. New action_log table records each instrumented action with a small JSON reversal plan (updates / increments / deletes over a whitelisted set of tables). A floating Undo button (shown after login on every main-app page) previews the user's most recent un-undone action, confirms, then reverses it and refreshes the page. Instrumented the 8 Station Leader Hub logs (glue-mix, laminating, cold-press, repair, sanding, hot-press, grading, packing): undo deletes the log row and, for the press/grading stations, restores the prod_batch status it advanced. Scope is per-user, single-level, within 24h; the executor refuses non-whitelisted tables. Endpoints: GET/POST /api/undo/last. Verified e2e: laminating log -> undo removes the row; increment reversal (orders.produced_qty 30->25); per-user isolation; whitelist guard. Next phases: stock receipts / moves / NCG / scrap."),
    ("2.21.37", "2026-06-17", "QA/QC NCG: weighted-average regrade/resize cost + resize-to-new-SKU. Regrade/Resize now carry the NCG cost basis into the target's unit_cost via a weighted-average blend over the target's on-hand (FC+WH+WLWH), mirroring the FC regrade, instead of just adding stock at the target's old price. For a resize the consumed value spreads over the (smaller) yield, so the resized pieces get the right per-unit cost. value_recovered is now the inventory value retained (consumed_qty x NCG cost). Resize can also create a brand-new target SKU on the fly (_create_ncg_target): the disposition modal has a 'Resize into a new SKU' toggle with code/name/dimensions; type & unit inherit from the source, and an existing code is reused not duplicated. Verified: regrade 12 into 20@6 -> 32 @ 7.5; resize 12->yield 9 into new VSM2 -> 9 @ 13.3333; reuse-code no dup."),
    ("2.21.36", "2026-06-17", "QA/QC NCG Phase 3: flag-NCG from the UI (completes the feature). A red 'flag NCG' button now sits on every board/veneer row in Warehouse Raw Materials and on FC inventory cards (fc_stock>0). It opens a shared modal (in core.js): pick the source bucket (only locations holding stock are offered), qty (capped at available), reason + optional detail. Submitting deducts the qty from that bucket and raises a QA/QC review item with the material's cost carried in. Verified in-browser: flag 15 from WH -> stock 60->45, QA/QC pending count +1. Together with Phases 1-2 this delivers the full loop: FC/WH flag -> QA/QC portal review -> dispose/regrade/resize, plus consolidated production-scrap review."),
    ("2.21.35", "2026-06-17", "QA/QC NCG Phase 2: the QA/QC portal (qaqc.html, served at /qaqc, QA_QC|Managerial login like Finance). Dashboard with KPIs + badges (NCG awaiting check, scrap awaiting review, value held in NCG, value recovered). 'Raw-Material NCG' queue lists flagged items; a Review modal records partial/mixed dispositions (dispose / regrade / resize) with a target-material picker, live remaining-qty and recovered-value, splitting one item across outcomes. 'Production Scrap / LG' queue consolidates the existing batch scrap review (set disposition). Verified in-browser end-to-end: flag -> review -> REGRADE 8->VENB moves stock +8 / recovers value, remaining decrements. Also: list_ncg_items treats status=ALL as no filter. Next: flag-NCG buttons in FC inventory + WH raw materials."),
    ("2.21.34", "2026-06-17", "QA/QC NCG Phase 1 (backend foundation). New QA_QC role (added to the users.role CHECK + role-rebuild migration + Role class). New ncg_items + ncg_dispositions tables for raw-material non-conforming goods (veneers/boards condemned from FC sorting or WH handling). flag_material_ncg deducts qty from the source bucket (FC/WH/WLWH) into an NCG holding record; QA/QC review_ncg_item, then add_ncg_disposition records partial/mixed outcomes (DISPOSE write-off / REGRADE / RESIZE) that move stock into a target material at the same location and tally value_recovered, clearing the item when fully dispositioned. Endpoints: GET /api/ncg[/reasons], POST /api/ncg/flag, POST /api/ncg/{id}/review|disposition (QA_QC|Managerial), GET /api/qc/summary (badge counts). Verified: flag 30 -> 10 dispose +12 regrade +8 resize(yield 6); stock + value_recovered correct. Next: qaqc.html portal + flag-NCG buttons in FC/WH."),
    ("2.21.33", "2026-06-17", "Raw-material price is now single-entry. The purchasing officer enters the agreed unit price when issuing the supplier PO in the Finance/Accounting portal (per-PR, incl. each bundled PR); it's stored on purchase_requests.unit_cost. Received lots inherit that price automatically, so Warehouse no longer re-keys it — the receiving Unit Cost field is read-only and prefilled from the PO. receive_pr_shipment uses the PR price authoritatively (a Warehouse-passed value is ignored when the PR is priced; legacy unpriced PRs still fall back to a typed value). New: purchase_requests.unit_cost column; issue-po / issue-po-multi accept unit_cost / unit_costs; the receiving view exposes pr_unit_cost. Verified end-to-end (bundled PO with two materials priced 15.5 / 8.25; WH override ignored)."),
    ("2.21.32", "2026-06-16", "Finance users can be created + the finance dashboard loads. Creating a Finance-portal user (role=FINANCE) failed with a 500: the users.role CHECK constraint only listed the 4 production roles, so the INSERT violated it. Added FINANCE to the CHECK for new DBs and a one-time table rebuild migration for existing DBs (SQLite can't ALTER a CHECK; the rebuild preserves all users). Registered Role.FINANCE in main.py. Also fixed a pre-existing 500 in get_accounting_summary (the finance/accounting dashboard's first call): it queried grading_log.created_at, but the column is graded_at. Verified end-to-end: managerial creates a FINANCE user, that user logs in, and /api/accounting/summary, /stock-movements and /purchase-orders all return 200."),
    ("2.21.31", "2026-06-16", "BOM save 500 fixed + DB-lock-leak guard. Saving a BOM 500'd with 'FOREIGN KEY constraint failed': bom_lines.group_id is NOT NULL with an FK to bom_groups, but save_bom_for_sku fell back to a hardcoded group_id=1 when the 'Core Materials' group was missing — and on a fresh DB bom_groups was never seeded, so every line insert failed. Now the group is seeded in init_db (idempotent) AND created on demand in save_bom_for_sku. Separately, save_bom_for_sku and save_glue_recipe opened a write connection with no try/finally, so a mid-save error (bad FK, duplicate code) leaked the open connection and held the SQLite write lock — wedging later writers with 'database is locked' (a single failed save could brick writes app-wide). Both now wrap the body so the connection is always released. Verified: BOM saves on a fresh DB; a forced write error no longer wedges subsequent saves."),
    ("2.21.30", "2026-06-16", "WIP Import now handles Finished Goods. Set current_station=fg_warehouse to import opening FG. Two optional FG-only columns: `pcs` (actual pieces - wins over pallets x pcs/pallet) and `location` (FG | WLWH overflow). Previously a fg_warehouse WIP row imported but was auto-marked 'completed' by move_batch (which the FG stock view excludes), so it showed zero stock; now those batches are re-activated and stamped with pcs_actual + fg_location so they count as FG inventory. The name-checker validates pcs/location and flags them as ignored on non-FG rows. Template + import result table gained Pallets/Pcs/Loc columns. Verified: pcs wins (180 over 4x50), pallet fallback (3x50=150), bad location rejected, FG dashboard split FG 180(4)/WLWH 150(3)."),
    ("2.21.29", "2026-06-16", "Two raw-material bugs fixed. (1) FC-stock CSV upload 500'd ('Internal Server Error' / frontend 'Unexpected token I ... not valid JSON'): in upload_fc_stock the `mat = SELECT ...` lookup was mis-indented INSIDE the except block (after a continue, dead code), so on the normal path `mat` was undefined and `if not mat` raised NameError on every valid row. Dedented it; upload now returns 200 and sets/adjusts fc_stock per row, skipping unknown codes as row errors. (2) create_material never included `code` in its INSERT, so materials added via the Add Material UI had no SKU code — invisible to BOMs, FIFO and the FC-stock CSV (which all match by code). Added code to the INSERT (empty->NULL), mirroring the earlier update_material fix. Verified: create persists code; fc-stock CSV sets 250 and skips a bogus code cleanly."),
    ("2.21.28", "2026-06-16", "Glue formula Edit button fixed for good + stale-JS root cause. The pencil on a glue recipe could open a blank 'New Glue Recipe' instead of editing the row: bomGlueEdit relied on the editor re-finding the recipe in a shared cache via strict id match, which missed on a stale/empty cache or a number/string id mismatch and silently fell through to New. Now bomGlueEdit hands the recipe OBJECT straight to gmOpenRecipe; gmOpenRecipe accepts an object|id|nothing, matches ids string-safely, refetches once on a cache miss, and shows an error instead of silently opening New. ROOT CAUSE of why the earlier 2.8.4 fix 'still persisted' on the hosted server: /static/*.js was served with no Cache-Control, so browsers heuristically cached app JS and operators kept running OLD code after deploys. Static JS/CSS now sends 'no-cache, must-revalidate' (ETag-validated), and portal_*.js loads with a ?v=<version> tag so every deploy guarantees a fresh fetch. Verified in-browser: empty-cache+string-id and object paths open the right recipe; a bogus id errors instead of opening New."),
    ("2.21.27", "2026-06-16", "WLWH Phase 4: FG <-> WLWH location moves. Finished-goods batches can be parked at WLWH overflow storage and brought back, all self-service for WH staff. The batch never leaves FG inventory (department stays fg_warehouse) - only the new batches.fg_location column changes, so FG counts and PO fulfillment are unaffected. FG dashboard stock table now splits 'At FG' vs 'At WLWH' pallets per SKU; a Move dialog lists the SKU's batches with checkboxes and a destination toggle. New: batches.fg_location, fg_location_moves log, list_fg_batches/move_fg_batches/get_fg_location_log, GET /api/fg-warehouse/batches, POST /api/fg-warehouse/move-location, GET /api/fg-warehouse/move-log. Completes the WLWH build (Phases 1-4)."),
    ("2.21.26", "2026-06-16", "Full TH/ZH translations linked. Regenerated the I18N dict in frontend/js/i18n.js from the reviewed translations workbook (108 -> 903 entries, all keyed by the exact English source string, no blanks, none of the old hand-written keys dropped). The i18n engine already auto-translates static UI by exact-match lookup, so populating the dict IS the link - no code changes needed. Added scripts/gen_i18n.py to re-run the import whenever the workbook is updated."),
    ("2.21.25", "2026-06-12", "WLWH Phase 3: receive-to-location. The Raw Material Receiving modal now offers a 'Receive into: WH / WLWH' choice for boards & veneers (hidden for other types). receive_pr_shipment / quick_receive_for_pr take a destination param and bump current_stock (WH) or wlwh_stock (WLWH); the material lot is still created the same way (FIFO), only the bucket differs. WLWH is guarded to boards/veneers only - a consumable received 'to WLWH' falls back to WH. Endpoints' ReceiveIn/QuickReceiveIn gained an optional destination (default WH). Verified: board received 40->WLWH / 25->WH; consumable WLWH attempt -> WH. Next: (4) FG->WLWH."),
    ("2.21.24", "2026-06-12", "WLWH Phase 2: WH<->WLWH stock movements + clearer location UX. (UX) Raw Materials board/veneer rows now use explicit Location | Qty column pairs (a 2nd pair appears when stock is split across WH and WLWH) instead of the inline 'WH ###'; consumables keep a single Stock number (no location). (Movement) New wh_stock_transfers log + wh_move_stock(material, from, to, qty) — WH staff self-service move of boards/veneers between WH and WLWH; validates the source bucket, shifts qty, logs it. Endpoints POST /api/warehouse/move-stock (WAREHOUSE/MANAGERIAL) + GET /api/warehouse/move-log. A Move (<->) button on each board/veneer row opens a from/to/qty modal showing available stock. Verified: move 30 WH->WLWH (100->70 / 0->30), over-move blocked, logged. Next: (3) receive-to-location, (4) FG->WLWH."),
    ("2.21.23", "2026-06-12", "WLWH multi-location stock — Phase 1 of 4. New materials.wlwh_stock column: a second physical storage location for boards + veneers, alongside current_stock (WH) and fc_stock (FC). Raw Materials board/veneer rows now show the per-location breakdown (WH headline + 'WLWH n / FC n'). Migration is idempotent. Next phases: (2) WH<->WLWH internal movement requests (WH self-service), (3) receive-to-location (WH or WLWH), (4) FG -> WLWH movement."),
    ("2.21.22", "2026-06-12", "Veneer regrade now adjusts cost via weighted average (was: none — regraded pcs silently inherited the target's catalog cost, inflating/deflating inventory value). The regraded pieces carry the SOURCE's unit_cost into the target, and the target SKU's single unit_cost is re-blended over its TOTAL on-hand (fc_stock + current_stock/WH; there is no separate PSP bucket today): new_cost = (target_total*target_cost + qty*source_cost)/(target_total+qty). Correct for up- AND down-grades; the source cost is unchanged. Cost change is recorded on veneer_regrade_log (from_unit_cost, to_unit_cost_before/after) for audit, and shown in the success toast. Verified: empty target carries source cost (15->10), target with 20@15 + 10@10 -> 13.3333. (Also: the regrade button was failing on the server because the final query reads grade/species columns missing pre-2.21.20 — fixed by that migration.)"),
    ("2.21.21", "2026-06-12", "FC / Cutting is now its OWN line, not a station owned by P01/P02/P37. FC is an independent upstream prep operation that feeds the lamination lines. Changes: added 'FC' to manufacturing_line (line_type 'prep', sort_order 0); the FC line owns the single FC/Cutting station; the main lines no longer get a per-line FC station (the seed skips it) and any existing P01.fc/P02.fc/P37.fc stations are retired (deactivated, not deleted, so history is intact). The main lines still list fc at seq 0 in their flow, so a batch still starts at FC. Station Hub line selector now includes FC (prep) so cutting work can be logged there; FC stays off the main line board. Idempotent migration runs on startup. Verified on fresh + simulated-existing DBs."),
    ("2.21.20", "2026-06-12", "Fix veneer/board imports filing as 'other' + warehouse-dashboard 500. (1) The Veneers/Boards templates have no 'type' column and all three Data Tools drop zones POST to the same /api/upload/inventory with no hint, so bulk_upsert_material defaulted every row to 'other'. The endpoint now takes default_type and the Veneers/Boards zones pass 'veneer'/'board' (normalised to veneer_sheet/core_board); a row's own type still wins (consumables template keeps its type column). (2) The materials table was missing 18 columns on fresh DBs (name_th, name_zh, thickness/width/length_mm, species, grade, board_type, fsc, acc_code, etc.) that existed only on long-lived dev DBs — so GET /api/warehouse/dashboard 500'd on m.name_th AND the importer silently dropped all veneer/board specs/dimensions. Added them to the _migrations ALTER list (idempotent). After deploy, re-drop the veneer/board CSVs into their zones to re-file + capture dimensions."),
    ("2.21.19", "2026-06-12", "deploy.ps1 now reads the listen port from config.py (.env) instead of hardcoding 8000. The restart succeeded but the health check polled localhost:8000 while the server runs on 8001 (from .env), reporting a false 'Service did not become healthy on port 8000'. Now, unless -Port is passed explicitly, deploy.ps1 resolves PORT from config (same as start.bat / install_service.ps1) so the health check and the port-process kill target the real port."),
    ("2.21.18", "2026-06-12", "deploy.ps1 now supports Task Scheduler deployments (not just NSSM service). Restart step 3 is runner-agnostic: (a) restart the Windows service if one is registered, else (b) restart a scheduled task — found by new -TaskName param or auto-detected by scanning Get-ScheduledTask actions for uvicorn/main:app/start.bat/pv-erp — via Stop-ScheduledTask + kill-process-on-$Port (in case the server detached) + Start-ScheduledTask; else (c) clear error listing how to pass -TaskName / find the task / start manually. Context: the NSSM service fails on this server because Python is installed per-user (Administrator profile) so the SYSTEM/service account's py launcher reports 'No Installed Pythons Found' — Task Scheduler running as Administrator avoids that, hence first-class support here."),
    ("2.21.17", "2026-06-12", "Raw Material Receiving is read-only for non-receiving roles. rrecRender now only renders the Receive / Walk-in Receive buttons when the signed-in role is WAREHOUSE or MANAGERIAL (via getCurrentUser); Production Planning sees the arrivals, quantities, ETAs and statuses for planning but no action buttons (rows show '—'). Complements 2.21.16 which exposed the page to Planning. Note: the receive endpoints themselves are require_auth (not role-gated) — this is a UI control; backend tightening can follow if desired."),
    ("2.21.16", "2026-06-12", "Show Raw Material Receiving to Production Planning so planners can see incoming-material timing for production planning. Added 'raw-receiving' to ROLE_PAGES.PRODUCTION_PLANNING and PRODUCTION_PLANNING to the NAV_SEC_ROLES['nav-raw-receiving'] override (the link was double-gated). No loader/endpoint change: Planning already loads the warehouse portal bundle (rrecLoad) and GET /api/shipments only needs auth. Receiving is read/plan visibility for planners; the actual receive action remains warehouse-gated on the backend."),
    ("2.21.15", "2026-06-12", "Material Lots: show the unit when receiving a lot. The 'Receive New Material Lot' modal already captured the FIFO quantity, but the unit was a blank free-text box. Now picking a material auto-fills a read-only Unit field and shows the unit next to 'Qty received (…)', so receivers know what unit the FIFO quantity is in. lotMatPicked() reads the unit from the materials cache (_prMaterials) on select and on modal prime. Clarified labels (Qty received *, 'for FIFO' placeholder). Frontend-only. NOTE: quantity lives in the lot-receipt modal, not the separate 'Upload Material PDF' modal (which only attaches a cert to a material/lot)."),
    ("2.21.14", "2026-06-12", "Rudimentary customer CRM with enforced registration. New 'customers' table (name unique, contact_person, phone, email, address, notes, is_active) + a Customers page (Planning) to create/edit customers and view each one's order 'filing' (their purchase orders). create_purchase_order now REQUIRES the customer to be registered first — raises ValueError -> HTTP 400 'Customer X is not registered'. Order Intake's free-text customer field is now a dropdown of registered customers (custFillSelect on new/edit PO). Endpoints: GET/POST /api/customers, PUT /api/customers/{id}, GET /api/customers/{id}/orders. Idempotent boot seed auto-registers customers already named on existing POs so the rule never rejects pre-existing data; renaming a customer cascades to their POs. Page gated to Managerial + Production Planning."),
    ("2.21.13", "2026-06-12", "Opening WIP import — CSV upload to seed in-progress jobs at go-live. New 'Opening WIP Import' page (Planning > Products & BOM) takes a CSV (sku_code, line, quantity, current_station, batch_ref?, po_ref?) and, per row, creates a production order + batch placed at its current station (via create_production_order -> release -> move_batch) so the floor's in-flight goods appear without replaying history; materials are NOT deducted. VALIDATE-FIRST 'name checker': import_wip_batches(rows, commit) always returns a per-row report with blocking errors + closest-match suggestions (difflib) for bad SKU/line/station names, so nothing is lost to typos; commit only writes rows that pass. Endpoints: GET /api/upload/template/wip, POST /api/upload/wip?mode=validate|commit. Page gated to Managerial + Production Planning."),
    ("2.21.12", "2026-06-10", "Expose Lots & Documents and Traceability in the main app for Production Planning (and Managerial). Both pages existed (page divs + loaders in portal_warehouse/accounting) but had no sidebar link in index.html and weren't in any role's ROLE_PAGES, so they were only reachable via the standalone /finance portal. Added two nav links under Planning > Products & BOM (data-page=lots-docs, traceability) and added both page ids to ROLE_PAGES.PRODUCTION_PLANNING + .MANAGERIAL. No backend/loader change needed — Planning already loads the planning+warehouse+accounting portal bundles and the lots/docs/traceability endpoints aren't Managerial-gated."),
    ("2.21.11", "2026-06-10", "Service-install/deploy script robustness. (1) install_service.ps1 had a parse error (non-ASCII box-drawing/em-dash chars that PowerShell 5.1 chokes on, plus a $BindHost:$Port drive-ref bug) so it couldn't run — both fixed (ASCII-only, ${BindHost}:$Port). (2) install_service.ps1 now POLLS for the old service to actually disappear after nssm remove (up to 30s) instead of immediately recreating — fixes the 'CreateService(): the specified service has been marked for deletion' cascade when a handle (open Services.msc/Task Manager) delays deletion; gives a clear close-handles/reboot message if it stays stuck. (3) Both scripts refresh PATH from the registry at startup so git/py/nssm are found even when the shell was opened before they were installed. (4) deploy.ps1 gives a clear 'run install_service.ps1 first' message (and wraps Restart-Service in try/catch) when the service isn't installed yet, instead of a raw 'Cannot open service' error."),
    ("2.21.10", "2026-06-10", "CRITICAL: fix material bulk import (and material create/update) crashing. The helpers _mat_by_id() and _mat_by_code() in database.py were broken stubs that called THEMSELVES — `def _mat_by_code(conn,code): return _mat_by_code(conn,code)` — so every call hit 'maximum recursion depth exceeded'. bulk_upsert_material (CSV import) and the single-material create/update/get paths all use them, so importing materials failed on every row (surfacing in the browser as 'Failed to fetch' when the worker died) and they've been broken since the helpers were introduced. Both now actually query materials by id/code and return the row (or None). Verified: a 205-row consumables/glue/packing/other CSV imports 0-errors with correct type normalization (Consumable->adhesive, Glue and additives->glue_formula, packing, other)."),
    ("2.21.9", "2026-06-10", "Extend the multi-script font fix to Chinese. The 2.21.8 stack added Thai fonts but none of them (Segoe UI/Leelawadee UI/Tahoma/Noto Sans Thai) carry Chinese glyphs, so 简体中文 only rendered via the browser's nondeterministic last-resort CJK fallback. Added 'Microsoft YaHei' (the Windows Simplified-Chinese UI font) to the stack: 'Segoe UI','Leelawadee UI','Microsoft YaHei',Tahoma,'Noto Sans Thai','Noto Sans SC',system-ui,sans-serif. Per-glyph fallback now routes Latin->Segoe UI, Thai->Leelawadee UI, Chinese->Microsoft YaHei — all Windows-bundled, still no web-font download. styles.css + finance.html + admin.html."),
    ("2.21.8", "2026-06-10", "Fix Thai text rendering. The font stack was 'Segoe UI', sans-serif with no Thai-capable font, so Thai glyphs (and especially the stacked vowels/tone-marks) fell back unpredictably and rendered poorly. Stack now 'Segoe UI','Leelawadee UI','Noto Sans Thai',Tahoma,system-ui,sans-serif — Latin/numbers stay in Segoe UI while Thai characters fall through per-glyph to Leelawadee UI / Tahoma, both of which ship with Windows so it works offline on the factory LAN (no web-font download). Applied to styles.css (main app) + finance.html + admin.html. Charset was already UTF-8 (not a mojibake issue). Frontend-only — hard-refresh to pick up."),
    ("2.21.7", "2026-06-10", "Fix .env PORT/HOST being ignored at startup. The server is launched via the uvicorn CLI with explicit --host/--port, and both start.bat and install_service.ps1 hardcoded those from shell env vars (default 0.0.0.0:8000) — which never see .env. config.py loads .env but only affects code that reads config.PORT (CORS etc.), not the actual bind, so changing PORT in .env did nothing. Both launchers now resolve HOST/PORT from config.py (the single source that loads .env) before building the uvicorn args: start.bat queries it via py -c; install_service.ps1 does the same unless -BindHost/-Port are passed explicitly (those still win). Verified config reflects an overridden PORT."),
    ("2.21.6", "2026-06-10", "CRITICAL: fresh databases now bootstrap fully. init_db() could not build a new DB from scratch — 27 tables (manufacturing_line, users, user_sessions, skus, bom_groups, bom_lines, packing_skus/lines, all station logs, glue_mix_log, mfg_order/prod_batch, consumable_request, dept_cost_ledger, employee, suppliers, login_log, user_departments, ncg_reason, production_table/prod_machine) existed only on long-lived dev DBs from old schema revisions and were never in the CREATE block, so a clean server install crashed at startup ('no such table: manufacturing_line') and, past that, had no users table to log into. All are now created in init_db (IF NOT EXISTS — existing DBs untouched; migration-added columns inlined so the _migrations ALTERs no-op). Added _seed_default_admin: a brand-new DB (0 users) gets a bootstrap admin/admin MANAGERIAL account so a fresh install can be logged into (DEPLOY.md §2.6 — change it immediately). Verified: fresh init_db builds all 71 tables, seeds 6 lines + admin, login verifies, fully idempotent."),
    ("2.21.5", "2026-06-10", "Factory Assistant files now have their own dedicated store. Generated xlsx exports were written under backups/factory_assistant (nested in the DB-snapshot tree); they now live in a separate config.FA_DIR (default fa_storage/, overridable via the FA_DIR env var) — independent of docs_storage (material PDFs) and backups/ (DB snapshots), so it can be sized, cleaned, or relocated on its own. EXPORT_DIR points at FA_DIR; the download endpoint follows automatically; existing exports migrated; fa_storage/ gitignored; .env.example, config summary, and DEPLOY.md directory map updated."),
    ("2.21.4", "2026-06-10", "Fix Factory Assistant Excel download returning {'detail':'Authentication required'}. The export endpoint is Managerial-gated (reads the X-Auth-Token header), but the download chip was a plain <a href> / new-tab navigation, which the browser issues WITHOUT the header -> 401. New core.js authedDownload(url, filename) fetches the file with the X-Auth-Token header, then saves the returned blob via a temporary object URL (no token in the URL). The FA export chip is now a button wired to it. Verified: endpoint 401s without the header and serves the xlsx (200) with it."),
    ("2.21.3", "2026-06-10", "Fix Factory Assistant maxing out tokens on Excel exports. export_to_excel required the model to pass the entire dataset back as the `rows` argument, so after query_database returned N rows the model had to re-emit every row as tool input — blowing the 4096 output-token cap and failing on anything but tiny results. The tool now takes a `sql` argument: the model passes the SELECT and the SERVER runs it (shared guarded _run_select, same 10k-row cap + write rejection) and writes the rows. `rows` kept as a fallback for small hand-built tables. Tool description + system prompt now instruct the assistant to export via sql and never paste rows."),
    ("2.21.2", "2026-06-10", "Traceability document hygiene. (1) Stop flooding the report with purchasing paperwork: the purchasing flow auto-attaches a SUPPLIER_PO/PO document to a material on every PR (one board had 252 SUPPLIER_PO rows the user never uploaded), so get_po_document_trace now EXCLUDES doc_type SUPPLIER_PO/PO and only surfaces quality/cert docs (COA, MSDS, SUPPLIER_CERT, INSPECTION, OTHER…). (2) De-dupe identical files (same stored_path registered once per PR). (3) Each doc now carries an 'available' flag (file actually present in DOCS_DIR); the UI renders available certs as clickable badges and collapses missing ones into a muted 'N not on this server' note instead of a link that 404s with 'File missing on disk'. doc_count now = downloadable PDFs; new missing_count surfaces recorded-but-absent files; the ZIP bundles only files present on disk."),
    ("2.21.1", "2026-06-10", "Shared glue-mix linkage for traceability. Operators group identical glue recipes from DIFFERENT sales POs into one mix, but glue_mix_log only stored one primary batch_id (the rest lived in a free-text notes tag), so only the primary batch's PO saw the actual mix. New glue_mix_batches link table records EVERY batch a mix served; log_glue_mix_with_stock + log_glue_mix now write one link row per batch (frontend sends batch_numbers[] on glue-mix-confirm), and get_po_document_trace matches mixes via the link table OR the legacy batch_id. Idempotent boot-time backfill seeds the table from existing rows' primary batch_id + parsed '[Shared mix across batches: …]' notes. Result: a shared mix is now attributed to all participating POs (verified: a mix spanning two POs shows as 'glue mix' on both), not just the first."),
    ("2.21.0", "2026-06-10", "Traceability reworked to be document-centric. The old report leaned on the sparsely-populated batch_material_lots ledger and exported a useless CSV. Now get_po_document_trace resolves a sales PO -> its production orders/batches -> every BOARD (BOM base-board line), VENEER (FC grade-mix release prod_order_veneer_alloc + confirmed-veneer fallback) and GLUE (glue_mix_log.actual_components, then recipe ingredients, then BOM glue-recipe fallback) material that fed it, and pulls ALL PDFs attached to those materials (material-level certs + lot docs). Page now shows Boards/Veneers/Glue sections with per-material PDF badges + counts; new 'Download all PDFs (ZIP)' bundles every file foldered by section with an index.csv manifest (GET /api/export/traceability/po/{id}/zip), replacing the CSV export. Fixed two real linkage bugs found en route: glue_mix_log keys batches by batch_number string (not id), and glue_recipes.material_links is {name: material_id} (ids are the values)."),
    ("2.20.0", "2026-06-09", "Per-BOM waste factors at FC. The hardcoded flat 5% waste applied to every board AND veneer in get_fc_material_requirements (the bom_lines branch) is gone — each board/veneer BOM line now carries its own waste_factor. New bom_lines.waste_factor column (idempotent ALTER + one-time seed: veneers seq 2/3 → 0.05, boards seq 1 → 0, only where NULL so existing veneer behaviour is preserved while boards drop to true 0% waste). FC requirement = qty × pcs/pallet × (1 + line waste). BOM Builder gains a 'Waste %' input on Base Board (default 0), Face Veneer + Back Veneer (default 5); save_bom_for_sku persists them, get_structured_bom surfaces them, and editing a BOM pre-fills the saved values."),
    ("2.19.1", "2026-06-09", "Station Hub stock movements: Issue/Use + Adjust now pick ONLY from the station's current stock (built from _stStock, not the full consumable catalog) — can't issue/recount what you don't hold; Issue is also guarded against exceeding on-hand. Records the date+time the consumable was actually issued/used (new station_stock_movements.occurred_at column; modal date/time relabelled 'Issued'/'Count', defaults to now, precise time input); movements history shows occurred_at. Dropped the warehouse-style [needed:] notes tag."),
    ("2.19.0", "2026-06-09", "Factory Assistant upgrade: (1) Persona — system prompt rewritten as a 30-year veteran wood-panel production manager (NCG root-cause, Theory of Constraints, veneer grading, supplier + Thai-factory context; direct, data-first, opinionated). (2) Memory — new fa_conversations + fa_knowledge tables; every turn saved; system prompt enriched each call with last-20 session messages + top-10 keyword-matched operational facts; 6th tool save_knowledge (assistant_observed); manager endpoints POST/GET /api/factory-assistant/knowledge; chat now carries session_id. (3) Frontend — per-session UUID, Knowledge Base panel + Add-knowledge form on the FA page. New CLAUDE.md documents architecture, tables, endpoints."),
    ("2.18.1", "2026-06-09", "Fix Factory Assistant 'internal server error' (anthropic.APIConnectionError). Root cause was host-injected ANTHROPIC_AUTH_TOKEN/BASE_URL/CUSTOM_HEADERS env vars hijacking the SDK — an EMPTY AUTH_TOKEN made it emit 'Authorization: Bearer ' which httpcore rejects. New config.make_anthropic_client() strips the bogus empties and pins base_url to the public endpoint with x-api-key auth; factory_assistant.py + claude_ai.py now build their clients through it. No-op on a normal server (those vars aren't set there)."),
    ("2.18.0", "2026-06-09", "Station consumable-request overhaul. (1) Production line is now hard-set from the Station Hub line selector — per-line departments must have a line; centralised depts (packing/fg_receiving/fg_warehouse) request line-less. (2) Request-from-Warehouse modal rebuilt: consumable type filter, multiple material lines, needed-by date+time (required). (3) Two-step receive — warehouse fulfils, station confirms receipt via 'View my open requests' → deposits into station_stock (fixes 'fulfilled stock never showed at the department'). (4) 'View my open requests' now opens a station-scoped list (was wrongly sending dept leaders to the warehouse queue page). (5) Standalone 'Receive' button removed from station stock (receipt flows from the open request); Issue/Use + Adjust kept"),
    ("2.17.3", "2026-06-09", "Stock guard added to consumable-request fulfillment — the one issue path that was missing it. fulfill_consumable_request now raises 'Insufficient WH stock: only N available' instead of silently clamping current_stock to 0 with MAX(0, ...), matching fulfill_fc_transfer_request and the glue-mix/VCMX paths. The /api/consumable-requests/{id}/fulfill endpoint wraps the ValueError as HTTP 400 so the warehouse UI shows the real message. Prevents over-issuing a consumable below available stock"),
    ("2.17.2", "2026-06-09", "Fix (real one): the 'Promise Error: loadDashboard is not defined' came from index.html's INIT IIFE — `(async()=>{await preload();loadDashboard();})()` — which fired at parse time, BEFORE any portal script loaded. Dropped the loadDashboard() call from the IIFE (preload() still runs); navigation to dashboard now happens via applySession after the portal is dynamically loaded. Also removed a leftover 'dashboard': loadDashboard registration in portal_admin.js's Object.assign that my last script missed because of unusual whitespace."),
    ("2.17.1", "2026-06-09", "Fix: Dashboard (loadDashboard) moved from portal_admin.js to portal_planning.js so PRODUCTION_PLANNING and DEPARTMENT_LEADER users don't hit a ReferenceError on landing — they download planning but not admin, and 'dashboard' was their default page. Now every non-warehouse role has loadDashboard available because every non-warehouse role loads portal_planning.js"),
    ("2.17.0", "2026-06-09", "Deployment hardening: scripts/install_service.ps1 (NSSM-based PVWoodERP Windows service — auto-start, restart on crash, log rotation), scripts/uninstall_service.ps1, scripts/backup_db.py (live online-backup of SQLite — service does NOT need stopping), scripts/install_backup_task.ps1 (daily 02:00 Task Scheduler job, runs as SYSTEM, keeps 30 snapshots), GET /api/health (anonymous liveness probe: ok/db_reachable/disk_free_mb/uptime_s/version), and new DEPLOY.md (replaces stale MIGRATION.md) covering prereqs, first-time install, update procedure, rollback, common issues, and a directory map"),
    ("2.16.0", "2026-06-09", "Portal split step 13 DONE: dynamic per-role portal loader. The four static <script src=portal_*.js> tags are gone; auth.js's applySession is now async and loadPortalsForRole(role) injects only the modules the signed-in role needs. WAREHOUSE users download 132 kB instead of 665 kB on first paint; managerial users still load all four. SPLIT_PLAN.md updated — 13 of 14 steps complete (step 14 = optional HTML-page-divs move into portal files, deferred)"),
    ("2.15.0", "2026-06-09", "Portal split steps 11+12 ~done: cross-portal helpers (prioBadge, statusBadge, lineBadge, populateSel, STATION_*/DEPT_*, slStatusBadge, slDeptBadge, escapeHtml) hoisted to core.js; 8 more loaders (Dashboard, Machines, Sales Orders, FG, Materials, Lots & Documents, Purchasing Hub, Traceability) distributed across the four portal files. index.html JS shrunk from ~18,500 to 5,854 lines (~32% of original); PAGE_LOADERS dict now wholly owned by portal modules. Step 13 (dynamic per-role portal loader) is the natural next step"),
    ("2.14.0", "2026-06-08", "Portal split major milestone: completed portal_planning.js (BOM cluster + SLH cluster + order-intake/line-board/kanban/prod-logs/prod-reports/forklift-report — now 14 pages, 372k), created portal_accounting.js (193 lines, 2 pages) and portal_admin.js (353 lines, 2 pages — Factory Assistant + Employees + User Management). index.html now 525k (started at 1.2 MB, ~44% of original). 27 page loaders owned by the four portal files; main inline script keeps the truly cross-cutting pages (dashboard, materials, fg, orders, machines, purchasing, lots-docs, traceability, …)"),
    ("2.13.0", "2026-06-05", "Portal split progress: portal_warehouse.js gained wq/rrec/frfl/fkDash/scrap (+1,218 lines); new portal_planning.js created with VCMX (chunk 1, 562 lines) and Material Shortfalls + FC Material Requests + FC Hub (chunk 2, 958 lines). index.html down from ~1.2 MB to 835k (~30% smaller). 8 page loaders self-registered out of inline script. SPLIT_PLAN.md updated"),
    ("2.12.0", "2026-06-05", "Portal split progress: extracted /static/js/nav.js (PAGE_LOADERS empty + navigateTo + loadPage; main script registers loaders via Object.assign), /static/js/auth.js (login + session + applySession + initAuth IIFE; loads at end of body to see main-script globals), and /static/js/portal_warehouse.js (the three new warehouse-portal pages — wh-dashboard, wh-low-stock, wh-open-prs — self-register via Object.assign(PAGE_LOADERS, {...})). SPLIT_PLAN.md updated with current load order and 13-step progress table"),
    ("2.11.1", "2026-06-05", "Stations: new `stations` table joins lines × departments — 27 rows seeded (8 per-line depts × 3 main lines + 3 centralised hubs with line_code NULL). UNIQUE(line_code, department_code) prevents duplicates. /api/catalog/stations endpoint (filter by line_code='' for centralised, by department_code, or both). dept_activities and station_presets gained line_id columns so per-station analytics can JOIN against stations going forward"),
    ("2.11.0", "2026-06-05", "Factory Assistant AI: Claude-powered chat surface (Managerial role) replacing the unrouted BOM-AI + Capacity placeholders. Five tools — query_database (SELECT only, PRAGMA query_only enforced, 10k row cap), list_tables, describe_table, read_server_log, export_to_excel. Excel files saved under backups/factory_assistant/ and served via /api/factory-assistant/export/{file}. Frontend chat page with transcript, exports tray, and 4 sample prompts. Also: frontend portal split foundation (extracted styles.css, i18n.js, core.js; SPLIT_PLAN.md documents the per-portal carve-up)"),
    ("2.10.0", "2026-06-05", "Lines/Stations DB foundation: new `departments` and `line_flow` tables, manufacturing_line extended with line_type ('main'|'aux') + sort_order; seeded 6 lines (P01/P02/P37 main + PUV/PVS/PSP aux), 11 departments, default flow for the 3 main lines; new /api/catalog/{lines,departments,line-flow,lines/{code}/flow} endpoints; frontend now fetches the catalog at preload and exposes catalogLineCodes/DeptLabel/DeptIcon helpers; 2 unambiguous line-list sites migrated (LINE_OPTIONS, SL_LINE_OPTIONS) — LINE_FLOW kept (PM2 conflict) and HTML dropdowns left for the portal split. Adding 'P38' is now one DB insert; 5 files no longer need editing"),
    ("2.9.0", "2026-06-05", "Cleanup chunk 3: backend `class Role` + frontend `const ROLE` identifier constants (catches role-string typos at parse/load time); _mat_by_id/_mat_by_code/_mat_id_by_code helpers in database.py with 5 inline sites migrated; hardcoded SND-01/CP-01/HP-01 fallback machine codes replaced with a clear 'no machine configured' empty state; formatters consolidated into 4 canonical (fmtNum/fmtMoney/fmtDate/fmtQty) with currency-aware fmtMoney(n,ccy) now backing _accFmtB (Thai Baht) + _accFmtU (USD) ahead of dual-currency accounting"),
    ("2.8.4", "2026-06-05", "Glue Recipe edit fix on BOM tab: clicking the pencil icon on a recipe row was opening a blank 'New Glue Recipe' modal because bomGlueEdit() set window._gmRecipes after fetching but _gmRecipes is a let-bound lexical (not a window property) so the modal's lookup hit an empty array. Now assigns _gmRecipes directly"),
    ("2.8.3", "2026-06-05", "BOM Builder fix: glue recipe selection now persists when editing an existing BOM. Was a race: openBomBuilder fired loadBomBuilder without awaiting, editBomCard ran bbLoadFg after a 150ms timer, and the late-finishing loadBomBuilder re-rendered the dropdown and wiped the selection. openBomBuilder is now an async function that awaits the picker fetch; the 150ms hack is gone"),
    ("2.8.2", "2026-06-05", "BOM fix: Glue BOM now displays on existing FG list (get_structured_bom switched from INNER JOIN materials to LEFT JOIN materials + LEFT JOIN glue_recipes; 93 of 125 BOMs were silently missing their glue); new-BOM Glue picker pulls from /api/glue-recipes (was loading raw 'Glue and Additives' ingredients); save_bom_for_sku resolves glue codes against glue_recipes.recipe_code with legacy materials.code fallback; UI base font-size dropped to 14px so the dense ERP doesn't need manual zoom-out"),
    ("2.8.1", "2026-06-05", "Cleanup chunk 2 (zero behaviour change for live paths): canonicalise glue API as /api/glue-recipes (delete /api/compound-skus, /api/glue-formulas, /api/glue duplicates); rename backend get_all_compound_skus->get_glue_recipes_summary, get_compound_sku->get_glue_recipe_detail, get_compound_skus_with_lines->get_glue_recipes_with_ingredients; drop dept-fc/fc-requests/packing-center router redirects (no callers); refactor 40-branch loadPage() if/else into PAGE_LOADERS dict (also fixed latent station-log double-chain bug); hoist 18 lazy from-database imports to module top; spawned task for non-functional Glue BOM add/delete-ingredient buttons. Audit 37/37 throughout."),
    ("2.8.0", "2026-06-05", "Cleanup chunk 1 (zero behaviour change): drop resync_glue_placeholder_prices no-op + /api/price-sync, drop ORDERED->PO_ISSUED write-side alias + ordered_at stamping (read paths still handle legacy rows), remove fcHubLoadInventory_legacy frontend stub, delete archive/debug/ (pre-server-log diagnostics) and scripts/_phaseB_cleanup.py (executed 2.5.0). Git repo initialised; this work is on branch cleanup/2.7.x"),
    ("2.7.6", "2026-06-04", "Material Requests page: always back-fill Stock-in-WH / WH-Stock columns from a live /api/materials snapshot fetched in parallel with the request lists, so the value matches Raw Materials even on deployments where the consumable-requests endpoint hasn't been redeployed with current_stock in its SELECT"),
    ("2.7.5", "2026-06-04", "Material Requests page: 'Remaining ฿' column replaced with 'Stock in WH' showing live warehouse stock per material (now matches Raw Materials page); shortfall flagged in red with 'short by N' hint when current stock < remaining outstanding; backend get_consumable_requests now joins current_stock/reorder_point/name_th so the row carries truthful inventory data"),
    ("2.7.4", "2026-06-04", "Warehouse Portal: rename Supply Queue → Material Requests (clearer label for requests from production lines), add pending count badge to sidebar, swap the 4th dashboard KPI to 'Material requests' linking to the same page, and label the right-column dashboard table 'Material requests this week' with click-through"),
    ("2.7.3", "2026-06-04", "Warehouse Portal bugfixes: Raw Material Receiving sidebar link now appears in /warehouse (NAV_SEC_ROLES no longer overrides portal allowlist for nav-links); new PRs always become visible after submit (Send PR clears Open-PRs category filter, New PR auto-switches filter to match the new request's category); Arriving/Exporting KPI tiles clickable to Receiving / Supply Queue"),
    ("2.7.2", "2026-06-04", "Warehouse Portal: category filter on Low Stock + Raw Material Receiving; New PR button on Open Purchase Requests (material picker modal with low-stock indicator, suggested qty, priority — covers all 6 raw-material categories)"),
    ("2.7.1", "2026-06-04", "Warehouse Portal: new Dashboard (KPIs + this-week receiving/exporting), Low Stock worklist with one-click Send PR modal (suggested qty = min×2 − current; covers consumable/glue/packing/others — boards & veneers stay with Planning), Open Purchase Requests view with category filter and delivery plan column"),
    ("2.7.0", "2026-06-04", "Warehouse Portal at /warehouse: sidebar locked to Raw Materials + Supply Queue + FG Warehouse regardless of signed-in role; empty section headings auto-hide; admins can visit /warehouse without being bounced back to /"),
    ("2.6.3", "2026-06-04", "Raw Materials page: drop standalone Packing table at bottom (packing now shows only via Packing or All tab); description column uses name_th when Thai language is active"),
    ("2.6.2", "2026-06-04", "Bulk import hardening: cp1252 encoding fallback, silent skip of trailing blank rows (Excel artifact), type alias normalization (Consumables→adhesive, Glue and additives→glue_formula, Packing→packing, Others→other); Data Tools card relabeled to 3 buckets (Veneer/Boards/Consumables)"),
    ("2.6.1", "2026-06-02", "Raw material CSV exports + templates: rename reorder_point→min_stock, add acc_code, name_th, fc_stock columns; auto_glue_code on veneers; dims on consumables; importer accepts both old + new headers"),
    ("2.6.0", "2026-05-29", "Glue BOM Builder is now in Bill of Materials → Glue Formulas tab (full editor); Glue Mixing recipes are read-only; batch logger captures per-ingredient actual kg + actual ฿ cost"),
    ("2.5.1", "2026-05-29", "Glue ingredients moved to dedicated 'Glue and Additives' category; BOM → Glue Formulas tab now redirects to the Glue Mixing editor"),
    ("2.5.0", "2026-05-29", "Glue Phase B: glue_recipes is now sole source of truth; placeholder material rows + compound_skus/compound_lines/compound_cost dropped; bom_lines.material_id nullable; legacy normalize/sync code removed"),
    ("2.4.0", "2026-05-29", "Glue Phase A: hide glue_formula placeholders from Raw Materials; 8 mis-typed ingredients reclassified; BOM lines dual-linked via new glue_recipe_id"),
    ("2.3.0", "2026-05-29", "Glue recipe BOM editor links ingredients to catalog materials; bug fix: editing material code now persists"),
    ("2.2.0", "2026-05-27", "i18n: language switcher (EN/TH/ZH) on main app + admin + finance portals; shell strings translated"),
    ("2.1.0", "2026-05-27", "Versioning system, persistent server logs, admin log-tail endpoint"),
    ("2.0.9", "2026-05-27", "Stock & Movements multi-material + required date/time; glue-mix catalog fallback"),
    ("2.0.8", "2026-05-27", "PVWood brand: logo SVG, green/brown theme, /warehouse portal, aux line assignment"),
    ("2.0.7", "2026-05-27", "Material requests: needed_time (AM/PM); WH queue filters; FG warehouse PO collapse"),
    ("2.0.6", "2026-05-27", "Aux lines (PUV/PVS/PSP) — request hub, forklifts + stock tabs enabled"),
    ("2.0.5", "2026-05-27", "Aux lines added to SLH; consumable + FC request flow extended"),
    ("2.0.4", "2026-05-26", "VCMX fixes: catalog pair-lookup, FC stock check, weighted avg, partial completion, traceability"),
    ("2.0.3", "2026-05-26", "VCMX BOMs moved under Bill of Materials; dim fields surface in FG BOM builder"),
    ("2.0.2", "2026-05-26", "VCMX make-to-stock: BOMs, production orders, VCMX-Lam station, cost rollup"),
    ("2.0.1", "2026-05-26", "Multi-material PR with split deliveries; ad-hoc stock-building PR upgraded"),
    ("2.0.0", "2026-05-26", "Priority + needed_by cascade across PO → shortfall → FC prep"),
]

def get_info() -> dict:
    """Return current version metadata as a dict (for /api/version)."""
    return {
        "name":        "PVWood ERP",
        "version":     VERSION,
        "build_date":  BUILD_DATE,
        "patch":       VERSION.rsplit(".", 1)[-1],
        "minor":       VERSION.split(".")[1],
        "major":       VERSION.split(".")[0],
        "today":       date.today().isoformat(),
        "changelog":   [{"version": v, "date": d, "note": n} for v, d, n in CHANGELOG[:20]],
    }
